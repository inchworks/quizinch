{{template "deck" .}}

{{define "slides"}}

<section class="slide">
	<div class="tick">{{.Tick}}</div>
	<h2>Interval</h2>
	<img src="/static/images/big-logo.png" />
	<h3>{{ .Organiser }}</h3>
</section>

{{template "break" .}}

{{end}}