{{define "break"}}

	{{if eq .BreakSlide "E"}}
	
		<section class="slide">
			<div class="tick">{{.Tick}}</div>
			<img class="logo" src="/static/images/big-logo.png" />
			<h3>{{ .Organiser }}</h3>
		</section>

		<section class="slide">
			<p>..</p>
		</section>

	{{else if eq .BreakSlide "P"}}
	
		<section class="slide">
			<p>..</p>
		</section>

	{{end}}

{{end}}